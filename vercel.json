{
    "builds": [
      {
        "src": "frontend/package.json",
        "use": "@vercel/node",
        "config": {
          "maxLambdaSize": "50mb"
        }
      }
    ],
    "scripts": {
      "build": "npm install --legacy-peer-deps && npm run build"
    }
  }
  